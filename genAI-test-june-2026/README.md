# TDT4258 - 2026 - RPi4setup README.md

A simple code example demonstrating possible vsCode setup for Raspberry Pi4. 

The folder contains a simple ARM v7 assembler program (assembler.s) and a C program (main.c). The C program calls asmAdd and checks the result. It then tests exam question no 17 from 2024.

Be sure to open vsCode with this folder as the current folder. Typing "make" in the terminal will compile the program. It can be run by typing "./test" in the terminal, og debugged by pressing F5 in the source code.


## TODO

* extensions needed ... (Try to run from empty installation)
* take snapshot to include on canvas-code-intro
  * command line snippet-tool
    * grim -g "$(slurp)" screenshot.png
 